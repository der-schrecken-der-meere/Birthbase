enum BirthdayFormMode {
    /** Allows creating birthdays */
    CREATE,
    /** Only allows to read birthdays */
    READ,
    /** Allows updating or deleting birthdays */
    UPDATE,
};

export { BirthdayFormMode };